Notes and scripts for RegEx Labs.
In this file, we downloaded three text files and moved them into a RegEx folder.
We were able to match text at the start/end of lines and match specific sets of characters (letters or digits)
This script also used grouping/alteration to find multiple patterns at once.
