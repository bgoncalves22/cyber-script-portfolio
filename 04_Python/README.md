#Reflection Sept 17: CMD Reflection - using Notepad++ , I learned to adjust user settings to batch files to run a command script. Notepad++ can be senstiive with forward slash and backslash when running a file. Adding REM (not case senstiive) prior to a sentence will not run the code. The REM option is a way to add notes into the code. When working in CMD we will use UTF-8 without BOM so it does not break the first line in CMD. Some programs did not run on the first try, troubleshooting required saving the changes in the script first and also removing the first line text. This did assist in the programming running properly.

Sept 24th: RegEx was introduced and there was a bit of difficulty locating the proper code that will find three digits at the end. After several attempts and practice pages, I learned the proper code is findstr /r "[0-9][0-9][0-9]$" codes.txt. RegEx is case sensitive and using the program while particpating in the class activity helped gained an understanding on how I downloaded three text files and moved them into a RegEx folder. I learned to match text at the start/end of lines and match specific sets of characters (letters or digits) Also used grouping/alteration to find multiple patterns at once.
